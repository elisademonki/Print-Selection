{
  "name": "print-selection",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --platform=browser --external:obsidian --outfile=main.js"
  },
  "devDependencies": {
    "esbuild": "^0.17.0",
    "markdown-it": "^12.0.6",
    "obsidian": "^1.4.0"
  }
}